# autodocs

Automatically update your docs using doxygen when you push.

## Setup

1. Make sure you've got a web server with PHP support set up.
2. Clone this repository into a directory accessible by it. 
3. Run `bash setup.sh`.
